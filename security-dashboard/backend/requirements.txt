# Dashboard backend dependencies. Lighter than the troubleshooter
# because there is no Claude API integration and no Fleet API client
# (the demo serves synthetic data). In production, add httpx for
# Fleet API polling and asyncpg/sqlalchemy for Postgres snapshots.

fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.0
